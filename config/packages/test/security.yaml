security:
    encoders:
        App\Entity\User:
            algorithm: auto
            # settings to make hashing very fast on the test environment
            cost: 4
            time_cost: 3
            memory_cost: 10
